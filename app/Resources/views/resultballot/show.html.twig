{% extends 'base.html.twig' %}

{% block body %}
    <h1>Resultballot</h1>

    <table>
        <tbody>
            <tr>
                <th>Numbervote</th>
                <td>{{ resultBallot.numberVote }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ resultBallot.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('resultballot_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('resultballot_edit', { 'id': resultBallot.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
